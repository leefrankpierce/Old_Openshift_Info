pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage('Build') {
            steps {
                script {
                sh 'uname -a'
                }
            }
        }
    }
}
